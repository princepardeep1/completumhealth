let baseUrl = `https://api.duckduckgo.com/`;
let imageUrl = `https://duckduckgo.com`;


const urls = {
  imageUrl,
  baseUrl,
  simpsons_Character: '?q=simpsons+characters&format=json',
  wire_Character: '?q=the+wire+characters&format=json',
};

export { urls };
